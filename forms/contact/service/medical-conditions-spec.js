describe('medicalConditions', function() {

  beforeEach(module('contact'));

  it('should ...', inject(function(medicalConditions) {

	//expect(medicalConditions.doSomething()).toEqual('something');

  }));

});