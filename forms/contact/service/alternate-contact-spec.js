describe('alternateContact', function() {

  beforeEach(module('contact'));

  it('should ...', inject(function(alternateContact) {

	//expect(alternateContact.doSomething()).toEqual('something');

  }));

});